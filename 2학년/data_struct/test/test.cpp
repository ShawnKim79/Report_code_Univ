// test.cpp : Defines the entry point for the console application.
/*

#include "stdafx.h"
#include <stdio.h>

 
{ 
	int a,t;     // 변수는 a(값 저장)와 t(스위칭);
	printf("숫자를 입력하세요:"); 
	scanf("%d",&a); 

	while(a){
		if(!(a%3))  // t가 3으로 나누어진다면 
			t=3;    // t는 3
		else if (!(a%5)) // t가 5으로 나누어진다면 
			t=5;         // t는 5
	////// 보 충 코 드 ///////////////////////////////////////////
		else
			t=a;
	/////////////////////////////////////////////////////////////

		switch(t){ 
			case 3: 
				
				printf("짝!");
				break; 
			case 5:
				
				printf("아자!");
				break; 
			default:
				
				printf("%d",t);
				break; 
		}
		printf("숫자를 입력하세요:"); 
		scanf("%d",&a); 
	}
		
	return 0; 
} */
